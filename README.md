# Sudoku-game-using-python-
A simple Sudoku game in Python (Tkinter) featuring a 9×9 grid, number pad, hints, pencil mode, undo/redo, and timer. It generates unique puzzles with adjustable difficulty levels and runs as a single file requiring only Python 3.x, no extra libraries.
